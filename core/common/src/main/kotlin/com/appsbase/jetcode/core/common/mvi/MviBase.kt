package com.appsbase.jetcode.core.common.mvi

/**
 * Base interface for all UI states in MVI pattern
 */
interface UiState

/**
 * Base interface for all UI events/actions in MVI pattern
 */
interface UiEvent

/**
 * Base interface for all side effects in MVI pattern
 */
interface UiEffect

/**
 * Base interface for all intents/user actions in MVI pattern
 */
interface UiIntent
